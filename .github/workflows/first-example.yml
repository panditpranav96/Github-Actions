name: First Workflow

on: push

jobs:
    first_job:
            runs-on : ubuntu-latest
            steps:
            - name: Welcome Message
              run: echo "Ny First Github Actions Job"

            - name: List files
              run: ls

            - name: Read File
              run: cat ReadME.md
                